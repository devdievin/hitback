export enum TabMenuRequest {
  BODY = "Body",
  HEADERS = "Headers",
  AUTHORIZATION = "Authorization",
  QUERY = "Query",
}

export enum TabMenuResponse {
  RESPONSE = "Response",
  HEADERS = "Headers",
  COOKIES = "Cookies",
  HISTORY = "History",
}
